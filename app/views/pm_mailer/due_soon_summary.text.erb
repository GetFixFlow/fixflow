Hi <%= @user.full_name %>,

The following preventive maintenance schedules are due within the next 7 days:

<% @pm_list.each do |pm| %>
  - <%= pm.name %>
    Asset:    <%= pm.asset&.name || "N/A" %>
    Priority: <%= pm.priority.upcase %>
    Due:      <%= pm.next_due_at&.strftime("%Y-%m-%d") || "Not set" %>

<% end %>

Please log in to FixFlow to review and prepare for these upcoming PM tasks.

— FixFlow
